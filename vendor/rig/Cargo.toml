[package]
edition = "2024"
name = "rig"
version = "0.39.0"
description = "An opinionated library for building LLM powered applications (vendor local copy)"
license = "MIT"
repository = "https://github.com/0xPlaygrounds/rig"

[lib]
name = "rig"
path = "src/lib.rs"

[dependencies]
rig-core = { path = "../rig-core" }
