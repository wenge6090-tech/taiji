# plan — 转化协议（蓝图 → 计划）

> **转化层**：本文件**平时为空**，是「蓝图 → 计划」的转化协议 schema。任务需要计划时，套用本协议把 `Blueprint.md` 的设计转化为可执行的具体计划。
> 具体计划是**瞬态产物**——写入任务目录或会话输出，**不写回本文件**。
>
> 数据流：`Blueprint(设计) --本协议转化--> 具体计划 --执行--> 代码`；执行后经验固化回流 `AGENTS.md(避坑) / Blueprint(设计定论)`。

---

## 计划结构（schema）

一次转化产出一份计划，含五段：

| 段 | 内容 | 来源 |
|----|------|------|
| **目标** | 一句话说清要做什么 | 用户意图 + Blueprint 定位 |
| **模块清单** | `file — 说明`，精确到文件粒度 | Blueprint 架构图（模块边界） |
| **接口签名** | 关键函数 / 类型签名 | Blueprint 部件图 + AGENTS.md 契约 |
| **依赖顺序** | 先 X 再 Y，附原因 | Blueprint 数据流方向 |
| **验收标准** | 可测量：测试命令 / 编译检查 / 冒烟步骤 | AGENTS.md 环境信息（`cargo test` 等） |

---

## 转化规则

1. 先读 `Blueprint.md`，定位与本次变更相关的设计章节（模块边界、数据流、类型契约图）。
2. 再从 `AGENTS.md` 核对**当前实现事实**——计划不得与已实现契约冲突；接口签名引用 AGENTS.md 既有契约，不重复造规则。
3. 模块清单精确到文件，依赖顺序沿 Blueprint 数据流方向：上游先于下游。
4. 产出计划后，用户确认即进入实现阶段。

---

## 阻塞点检查

计划中发现蓝图假设无法落地时，**在此指出，不要拖到实现阶段**：

- **模块拆分不合理**——边界与 Blueprint 架构图冲突；
- **类型约束不满足**——接口签名与 AGENTS.md 契约冲突；
- **依赖循环无法打破**——数据流反向成环。

---

## 生命周期

- **平时**：本文件保持空协议，不改写。
- **任务时**：套用 schema 生成具体计划（写任务目录或会话输出）。
- **完成后**：经验固化回流——边界条件 / 易错点沉淀到 `AGENTS.md` 末尾；设计级洞察回写 `Blueprint.md` 定论（需用户批准）。
